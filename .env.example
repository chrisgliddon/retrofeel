# Optional local setup inputs for the Google Desktop OAuth client.
# Never commit .env or OAuth tokens. The recorder itself reads oauth-client.json,
# not this file; transfer the official Desktop client JSON to oauth_client_path.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
